# manage-sport-app — entrada para Claude Code

La fuente de verdad del proyecto es `AGENTS.md`. Léelo y navega desde ahí.

@AGENTS.md

Protocolo compartido: `.agents/protocol/operating-protocol.md` (Regla del 95 %, Plan Mode,
ediciones quirúrgicas, guías de diseño obligatorias). Modelos por herramienta:
`.agents/orchestrators/modelos.md`. Comandos: `/auto` `/auto-git` `/spec` `/exec` `/exec-git`
`/exec-3` `/exec-3-git` `/research`. LF siempre · UI en español · Git en inglés
(Conventional Commits).
