-- Migración 017 ya aplicada en remoto antes del checkout de rama.
-- Placeholder para reconciliar el historial de migraciones de Supabase.
-- El contenido real (storage de documentos, pivotes documento_sedes/equipos) vive en la BD.
SELECT 1;
